<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_add_player"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="es.iesnervion.apol.nbav2.AddPlayer"
    tools:showIn="@layout/activity_add_player">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:id="@+id/imgPlayer_addPlayer"
        android:src="@drawable/silueta"
        android:onClick="changePicture"/>

    <!-- Nombre Jugador -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/imgPlayer_addPlayer"
        android:layout_marginTop="50dp"
        android:text="@string/namePlayer"
        android:id="@+id/namePlayer"
        />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/namePlayer"
        android:layout_alignParentEnd="true"
        android:id="@+id/editText"
        android:layout_toEndOf="@+id/namePlayer" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/namePlayer"
        android:id="@+id/pesoPlayer"
        android:layout_marginTop="50dp"
        android:text="@string/pesoJugador"/>
    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/pesoPlayer"
        android:layout_alignParentEnd="true"
        android:layout_alignBaseline="@id/pesoPlayer"
        android:id="@+id/spPeso"></Spinner>

</RelativeLayout>
